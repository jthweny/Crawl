# Crawl — legal pages

Static site for the Crawl iOS app's legal documents, served via GitHub Pages:

- `/privacy` — privacy policy (linked from the app and App Store Connect)
- `/terms` — terms of use

Source of truth for the wording lives in the app repo (`legal/*.md`); keep the
two in sync when either changes.
